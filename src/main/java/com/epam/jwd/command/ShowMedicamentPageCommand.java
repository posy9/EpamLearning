package com.epam.jwd.command;

public enum ShowMedicamentPageCommand implements Command {
    INSTANCE;

    @Override
    public CommandResponse execute(CommandRequest commandRequest) {
        //todo
        return null;
    }
}
